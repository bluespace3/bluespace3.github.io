---
title: '2026-09-16赛博日记'
categories: ["赛博日记"]
date: 2026-09-16T23:51:20+08:00
lastmod: 2026-09-16T23:51:20+08:00
draft: false
---
# 2026年9月16日 赛博日记

> 生成时间：2026-09-16 23:50 (Asia/Shanghai)

## 核心工作

### 💻 Claude Code 工作记录
> 全天共 **8 个 Cursor 会话 / 103 条消息**（Claude Code 会话 0 个），主要围绕两个测试自动化项目展开。

- **项目A（API 自动化测试）**，全天 5 个会话，今日主战场：
  - **API 用例编辑功能落地**：用例列表操作列新增「编辑」入口，可改标题、路径、默认账号、`requestJson`、`assertJson`；详情弹窗同款入口。保存走新接口 `POST /api/frontend/apiAutoCaseSave`（必须带 `id`，非法 JSON 前端拦截）——从后端接口到前端交互完整闭环。
  - **SVG 画布裁剪 Bug**：画布写死 `1000×640`，而对话生成的场景把节点一行横排（`x = 60 + i×200`），关联 8 条用例时终点 x≈1860，节点连线被裁。根因清晰：固定画布 × 单行布局。
  - **TAPD 人员导出**：从 `resources/userid.csv` 导出 11 人及 TAPD userid，产出可直接开 Excel 的 CSV。
  - **网络排查**：DNS 能解析到 Tailscale 地址、主机可 ping，但 8888 端口不通——转向核对已开放端口与实际在跑服务。
- **项目B（AI-TEST）**，2 个会话：
  - **1.1.1 版本发布并上云** ✅（92 条消息的大会话）
  - 本地 commit 成功但远程 push 失败（待跟进）
- **状态**：✅ 正常

### 📚 笔记产出（note-gen-sync 最近 24h 更新）

**1. NVFP4 生产栈 decode「掉速」排查（09-16 00:30，已定位，无需修复）**
- 现象：社区仓库升级 v0.1.7 后 decode 只有 ~100 t/s，疑回退（昨天 150+）
- 结论：**与升级零关联，是测试口径差异**。decode ≈ 步率(39-42 步/s) × MAL，内容决定 MAL：社区口径（长 prompt + 代码任务 + thinking + 长输出稳态）MAL 4.0-5.1 → 166+；普通短输出 MAL 2.6-3.1 → ~100。09-14 笔记结论"别拿普通文跟社区数字比"的现场重演
- 实证链：生产栈不引用仓库代码、vLLM venv 09-08 后未动、两次启动 cmdline 一致、GPU OC 在位——四项全排除
- 验收方法沉淀：同口径才可比 / 看 MAL（≥4=口径对上，2.5-3=普通文）/ c1 out1280 锚点 96-107（<90 才怀疑栈）/ 只认引擎打点
- 同日环境噪音两枚：客户端流式墙钟偏低；`no_proxy` 写 CIDR 导致 httpx 劫持（须 `trust_env=False`）

**2. HyperFrames（HTML→MP4 确定性渲染框架）研究与实测**（09-15 定稿，24h 内更新）
- 定位：Write HTML, Render video, Built for agents；同输入必同输出，CI 友好
- 安装坑：`npx init` 直走代理龟速 → 正确姿势是 `--ignore-scripts` + npmmirror 手动下 chrome-headless-shell；浏览器覆盖只认 `HYPERFRAMES_BROWSER_PATH`（`PUPPETEER_EXECUTABLE_PATH` 无效）；本机 ffprobe 是假的
- **3. 知识科普视频分层工作流规划**（承接实测笔记）：
  - 六阶段流水线：脚本(vLLM)→配音(edge-tts/IndexTTS)→素材→HTML 装配→render→审核门，全 CPU，与 GPU I2V 并行不打架
  - 核心契约：**旁白时长驱动一切**，`data-start/duration` 从 beats.json 程序化生成，不手拍数字
  - 画面四层策略：L1 静态排版为主力默认层，动画（L2/L3）只做"概念讲透的那一下"，占比 ≤30%；L4 写实默认关闭
  - 适用边界清晰：概念驱动进此线，画面驱动走 ComfyUI/I2V

### 📋 代办推送 / 🧠 记忆系统维护 / 🏠 回家通知
- 今日素材中无相关记录

## 思考与备忘

- **口径意识是性能测试的第一生产力**：这次 NVFP4"掉速"从怀疑回退到证实口径差，全程没改一行代码，靠的是排除链 + MAL 指标。以后所有 decode 数字必须带口径标签
- **API 自动化平台功能在收敛**：编辑闭环 + 画布可视化 + 人员映射，工具链在向"非开发也能维护用例"靠拢
- 待跟进：项目B 远程 push 失败原因；SVG 画布裁剪的修复方案（动态画布 or 换行布局）
- 环境坑家族 +1：no_proxy CIDR / all_proxy 劫持 / 假 ffprobe——本机代理配置是长期噪音源，值得做一次集中治理

## 今日摘要

一条主线做产品（API 测试平台编辑功能闭环 + 1.1.1 发布上云），一条主线做基建（NVFP4 掉速虚惊一场、口径差异实锤；HyperFrames 科普视频流水线完成规划）。Cursor 会话 8 个 103 条消息，产出 3 篇高质量笔记，无真正故障。

## 📊 自动化统计
- 捕获 Memory 数：0（memory/2026-09-16.md 不存在）
- 笔记更新数：3（NVFP4 掉速排查、HyperFrames 实测笔记、分层工作流规划）
- Todoist 完成任务数：不可用（本次运行无 Todoist 工具权限）
- Claude Code 会话数：8（Cursor 8 / Claude 0）
