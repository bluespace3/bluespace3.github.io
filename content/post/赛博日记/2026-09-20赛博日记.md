---
title: '2026-09-20赛博日记'
categories: ["赛博日记"]
date: 2026-09-20T23:50:55+08:00
lastmod: 2026-09-21T03:00:01+08:00
draft: false
---
# 2026-09-20（周日） 赛博日记

> 生成时间：2026-09-20 23:50（服务器侧）；23:55 合并更新：纳入 Mac 本机（mm）自动化事项

## 核心工作

### 💻 Claude Code 工作记录
今日无 Claude Code 会话，但有 **5 个 Cursor 会话**，全部集中在 API 测试平台项目（`e/code/ysbapitest`，下称「API 测试平台」），共 283 条消息：

- **14:03 / 148 条消息**：TDD 流程推进——先确认测试红灯状态，补齐最小实现，跑测试，只提交本次改动并推送。初始化文档与设计文档保持不变。
- **17:38 / 205 条消息**：修复节点编辑器丢字段 Bug——`editorIds` 列表漏了新增输入项（nodeWaitMs、nodePollTarget、nodePollInterval、nodePollTimeout、nodeDbAssert），切换节点前没有 change/blur handler 把值同步回 node 对象导致丢数据。定位后补齐绑定。
- **18:00 / 130 条消息**：SMS 登录流程修复——失败响应补充上下文（URL、HTTP 状态码、响应体）便于诊断，并增加空 token 校验。
- **18:21 / 79 条消息**：服务在 8899 端口起服务，theme.js 返回 200，用浏览器工具截图 index.html 验证「测试计划」Tab 的 shell 渲染（后端 API 预期 404 但界面结构正常）。
- **19:58 / 12 条消息**：补充设计文档。

**状态**：✅ 正常。主线是 API 测试平台从「测试计划/定时任务」功能（对应 Todoist 完成项）到节点编辑器细节修复的连续推进。

### 🎬 E02-CMP170HX 视频交付（本机主线）
今天另一条主线是 **E02-CMP170HX 破解锁科普视频**的渲染排查与交付：

- **硬核 Bug 三连**（sweep_static 全片静止陷阱排查）：
  1. 二分法排除 SVG、字幕轨、音频嫌疑后，最终破案：重写场景块时把 `#sc-s01_intro` 场景容器整个弄丢，8 幕全是裸 div，作用域选择器全部失配（92 条 "not found"）→ GSAP 时间线零绑定 → 全片静止。
  2. 第 5 帧解锁清单卡片空白：GSAP `from` 补间的 immediateRender 让列表项从 0 秒起隐藏，而出现被排到 88.5s，中间 14 秒全空。修复：列表项提前到卡片出现后立即入场。
  3. step2 beats 计时与实际音频有 **8.6s 幻影间隙**，root cause 是拼接时没加句间静音，按真实音频重建了时间轴（待后续在 step2 修复）。
- **交付**：E02-CMP170HX破解解锁-完整版.mp4（2分27秒，1080p，9.3MB，IndexTTS 女声配音），8 幕一镜到底，QC 抽帧+定点复检全部通过。
- **飞书发视频踩坑**：file/video 消息发送失败（错误 99991672），根因是飞书应用缺 `im:resource:upload` / `im:resource` 权限，图片消息不受影响。临时方案：feishu_doc upload_file 把 mp4 传进文档+下载链接；永久方案：给用户发了一键开通权限的链接。
- 另外重建了家机渲染环境、重启了 flashnext。

### 🛏️ 其他
- 23:00 前用户发「开机」指令，经 MacBook 中转广播 WOL 魔术包远程唤醒台式机 ✅。

## 🤖 Mac 本机（mm）自动化与维护

- **08:16 每日代办推送（微信 · Todoist）**：LangGraph 28 天学习计划持续进行中（Day 4+：Node/Edge 基础、条件路由、构建简单问答 Agent、第 1 周总结与实践；Day 8~14 State 深入/LLM 集成/Tool Calling/Checkpointing；Day 15~21 记忆机制；Day 22~28 LangSmith/生产环境/项目实战）。今日 1 项完成（api 测试平台-增加测试计划、定时任务等，见 Todoist 统计）
- **09:30 智能收藏每日回顾**：`smart-collect.js` 运行正常，2 张收藏卡均已毕业（reviewCount=5、nextReviewAt=null），无到期回顾，收藏库维持静默期
- **23:52 赛博日记 cron**：拉取服务器 23:50 版本，合并本机事项后写回本地+远程（双端合并模式第 4 天，运转顺畅）
- 主会话全天心跳安静（HEARTBEAT_OK），无异常通知
- **全天无人工对话**（飞书/微信通道均无用户消息；「开机」指令经服务器侧记录）

## 思考与备忘
- **二分排查的边界**：这次 sweep_static 排查先怀疑字幕轨密度、SVG 复杂度，走了弯路——当两个半区都失败时，应尽早怀疑「共同结构层」（容器丢失）而不是继续细粒度二分内容。
- **GSAP from 补间的 immediateRender** 是卡片空白的经典陷阱：from 状态在时间线 0 秒就渲染，除非显式控制 immediateRender:false 或提前安排入场。
- 飞书消息 media 发送失败时 receipt 只有 text part，要用 read 回查确认实际送达情况。
- 待修：hyperframes 流水线 step2 拼接未加句间静音导致音画时间轴漂移。

## 今日摘要
双线并行的一天：白天到晚上主要在 API 测试平台（Cursor 5 会话）上推进测试计划/定时任务功能并修节点编辑器、SMS 登录等细节；晚间完成 E02-CMP170HX 科普视频的渲染排障与交付，吃透了 sweep_static 静止陷阱和 GSAP 补间时序两个坑。Mac 本机侧自动化全线正常（代办推送、收藏静默、心跳安静），全天无人工对话。

## 📊 自动化统计
- 服务器侧捕获 Memory 数：3（服务器 memory/2026-09-20.md 及两个会话摘要）
- 笔记更新数：0（note-gen-sync 近 24h 无其他笔记更新）
- Todoist 完成任务数：1（api测试平台-增加测试计划、定时任务等）
- Claude Code 会话数：Claude 0 / Cursor 5，共 283 条消息
- Mac 本机 cron 会话：2（智能收藏回顾 09:30 + 赛博日记 23:52）
- Mac 本机 memory：1 条（2026-09-20.md，本次日记 cron 记录）
- Mac 本机人工对话：0（飞书/微信通道）
