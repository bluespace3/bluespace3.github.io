---
title: '2026-06-30赛博日记'
categories: ["赛博日记"]
date: 2026-06-30T23:56:05+08:00
lastmod: 2026-06-30T23:56:05+08:00
draft: false
---
# 2026年06月30日 赛博日记

> 生成时间：2026-06-30 23:55:01

## 📝 今日概要

基础维护与Bug歼灭日 —— 上午追溯了历史赛博日记被"智能收藏系统"污染的根因，下午修复了博客部署连续4天的级联故障，晚间推进了AI自动化测试项目的汇报PPT打磨与核心指标逻辑修正。

## 🔍 核心技术进展

### 1. 博客部署链式故障诊断与修复 🔥
- **故障范围**：6/27-6/29 共3篇赛博日记未发布到博客，自动部署 cron 连续4天失败
- **根因链**：6/27 日记 AI 生成失败 → 文件以 `--- 今日原始素材库 ---` 开头 → Hugo 误判为 YAML front matter → `failed to unmarshal YAML` → `deploy.sh` 的 `set -e` 在 Hugo 构建失败后立即退出 → 后续 git push 未执行 → 连锁阻塞
- **修复**：重写 6/27 日记为标准格式 + `hugo --cleanDestinationDir`（686页）+ 推送 GitHub
- **来源**：`memory/2026-06-30.md`

### 2. AI自动化测试汇报 PPT 精修
- 最后一页增加量化数据：用例编写 **2h → 15min**，维护 **0.5天 → 30min**
- 解决 flex 布局换行重叠问题（`0.5天` 文字较长导致 `line-height: 0.92` 叠行）
- **来源**：`memory/claude_2026-06-30.md`

### 3. 核心指标重构：误报率 → Bug发现率
- 计算公式变更：`标记为bug的日志数 / 全部失败记录`
- UI 全局文案替换：误报 → Bug，误报/非误报 → Bug/非Bug
- 隐藏失败日志表格中的"修复时长"列
- **来源**：`memory/claude_2026-06-30.md` + `memory/2026-06-30.md`

### 4. 用例跳过判定Bug修复
- **问题**：已通过用例（`run_success=true` 且满足预期）被误判为 ⏭️ 跳过
- **根因**：`detectExecutionCaseSkip` 在整段 stdout 扫关键词，不区分运行是否已通过
- **修复**：仅在运行未通过时才触发跳过检测（提交 `3dc45ec`）
- **涉及5个文件**：`executionCaseSkip.ts`, `webBuHistoryState.ts`, `handlers.ts`, `web-agent.service.ts`, `case_skip.py`

## 💡 深度洞察与经验教训

- **洞察：`set -e` 是双刃剑**。部署脚本用它保证"出错即停"，但一个非关键文件的 YAML 解析错误会级联阻塞整个发布管道4天。考虑对 Hugo 构建步骤做单独的错误隔离，而非一刀切 `set -e`。
- **洞察：AI生成内容与静态站点生成器存在天然兼容风险**。AI 生成 Markdown 时可能在文件头注入 `---`（素材分隔符），恰好命中 Hugo 的 front matter 语法。这是 AI 内容管线与 SSG 集成的一个隐藏陷阱。
- **教训：cron 日志是排查定时任务故障的生命线**。`/tmp/blog-deploy.log` 记录的详细错误信息是今天快速定位根因的关键。AI 生成管线应该在生成后做一次 front matter 合规校验，而非等到 Hugo 构建时才暴露。

## 🚀 未来行动设想
- [ ] 在赛博日记生成 cron 中增加 **front matter 校验步骤**：生成后检查文件头是否为合法 YAML front matter，不合格则自动修复或跳过该文件
- [ ] 在 `deploy.sh` 中将 Hugo 构建与 git push 解耦：Hugo 失败不应阻塞已成功构建的内容推送
- [ ] PPT 中"提质"列考虑补充具体数据（如缺陷发现数量、覆盖率提升等），使三列数据对称

## 📊 自动化统计
- 捕获 Memory 数：3
- 笔记更新数：0
