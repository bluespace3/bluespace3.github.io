---
title: '2026-09-19赛博日记'
categories: ["赛博日记"]
date: 2026-09-19T23:50:55+08:00
lastmod: 2026-09-20T05:00:02+08:00
draft: false
---
# 2026-09-19（周六） 赛博日记

> 生成时间：2026-09-19 23:50（Asia/Shanghai）；23:58 合并更新：纳入 Mac 本机（mm）自动化事项

## 核心工作

### 💻 Hermes 本机工作记录（来源：diary_inbox/hermes_2026-09-19.md）

今日 Hermes 本机共 5 个活跃会话，核心工作围绕**家庭基础设施域名化与远程访问链路**展开：

**1. Hermes Dashboard 域名部署 + Chat 报错硬核修复（338 条消息，今日主战场）**
- **部署链路**：本机 `hermes dashboard --host xxx.xxx.xxx.xxx --port 9119` 封装为 systemd 用户服务（enable + linger 开机自启，实测 kill -9 后约 5 秒自愈拉起）；云端 nginx（xxx.xxx.xxx.xxx）新建反代配置，CF 源站证书 + WebSocket 升级头 + 3600s 超时 + 关缓冲，全链路验证通过（域名 → 302 登录页 200）
- **硬核 Bug：Chat unavailable**：dashboard 打开 Chat 页时会在 `_internal/ui-tui` 跑 `npm install && npm run build` 做 TUI 预检，但 CN 版官方 zip 根本没打包这个工作区目录 → 必然失败。修复三步：创建最小 package.json（`type:module` + build 直通）、把 `_internal/ui-tui/dist` 符号链接到自带 `<runtime>/tui/dist/entry.js`、给 tui/ 目录也补 ESM package.json（踩坑点：node 按符号链接真实路径找 ESM 配置）。最终 PTY WebSocket 首帧吐出 TUI 真实界面，修复完成
- **密码找回**：admin 密码只存 scrypt 单向哈希，明文从未落盘（env/.env/会话转储全查过），无法反推 → 直接重置新密码并验证登录闭环 [凭据已脱敏]
- 教训已沉淀进 skill：修复在版本目录内，升级运行时后若 Chat 再报 unavailable，按 skill 第 9 条重放即可
- 中途插曲：SIGTERM 误伤——自己清理测试残留进程时杀掉了自己会话的子进程（网关 1554 未受影响，微信/飞书通道未断）

**2. dsh 域名接入与链路调优（157 条消息）**
- 链路定型：`手机/外部 → Cloudflare 边缘（橙云）→ 美国 VPS nginx → Tailscale 隧道 → 家里 dsh:3080`，真 HTTPS、CF 就近加速，避开 ts.net 的 DERP 绕路
- 配置要点：nginx 照搬 nc 域名模式（源证书 + SSE/WebSocket 长连接参数、关 buffering、1h 读超时）；dsh 加 `--trusted-host` 放行 Host 围栏
- 新版 dsh 加了启动令牌认证（401），需带一次性 token URL 过认证后 cookie 生效，cookie 按设备/浏览器各过一次 [token 已脱敏]
- 方案沉淀进 dsh-ops skill

**3. Hermes 每日事项同步链路重构（86 条消息）**
- 起因：hermes 每日汇总文件落进了笔记仓库，与赛博日记内容重复
- 修正（注意教训："不进仓库"≠"不上传"）：新链路 = 本地留档 `~/hermes_daily/` → 23:30 cron scp 直传服务器 `diary_inbox/`（仓库外）→ ssh stat 比对大小复验，失败微信告警；服务器侧日记任务 prompt 同步改为读 `diary_inbox/hermes_${TODAY}.md`
- 笔记仓库中历史 `memory/hermes_*.md` 维持删除，仓库恢复干净；skill `daily-diary-sync-hub` 已更新

**4. 腾讯知识库 / MemoryCore 评估（tui，16 条消息）**
- 知识库 :8421 运行正常：wiki 1406 页（version 93，持续增量），BM25 搜索 + 实体双链（in/out）均验证可用
- MemoryCore 记忆网关 :8420（未启动）评估：L1 自动记忆提取 / L2-L3 场景画像 / 混合召回 / 多 agent 共享大脑，但依赖本地 vLLM（qwen3.8-flash-next）占双卡推理资源、与现有笔记链路功能重叠、embedding 为 none 语义召回打折 → 结论：单 agent 场景暂不开启

**5. 其他**：微信通道执行关机脚本（1 分钟延迟断电）

## 🤖 Mac 本机（mm）自动化与维护

- **03:00 Memory Dreaming Promotion** cron 正常运行
- **04:49 回家通知服务触发一次**（周六凌晨——周五夜间晚归）
- **06:00 每周 OpenClaw 教程笔记同步检查**：飞书文档《【教程】Openclaw部署、使用、skill与三大实用玩法-Xuan酱-0305》revision_id 仍为 **2844**，与上次同步（2026-07-18）完全一致 → 无更新，未触发 scp；远程文件已确认存在且完整；状态记录至 `memory/openclaw-tutorial-sync-state.json`
- **08:16 每日代办推送（微信 · Todoist）**：LangGraph 28 天学习计划条目持续进行中（Day 4+：Node/Edge 基础、条件路由、简单问答 Agent 等）；今日无到期、无完成任务
- **09:30 智能收藏每日回顾**：`smart-collect.js` 运行正常，2 张收藏卡均已毕业（reviewCount=5、nextReviewAt=null），无到期回顾，收藏库维持静默期
- 主会话全天心跳安静（23:16 HEARTBEAT_OK），无异常通知
- **全天无人工对话**（飞书/微信通道均无用户消息）

## 待办备忘（来自会话提及）
- 9/21 截止：测试服鸿蒙、iOS、安卓互踢问题
- 9/30 截止：AI 自动化用例自修复（结合编程代理工具）

## 思考与备忘
- 今日主线是"把家里服务都挂上真域名"：hermes、dsh 两个服务完成了 CF 橙云 + VPS nginx + Tailscale 的统一接入模式，方案均可复用到下一个服务
- 修复类工作两次踩到"发行包缺陷"：官方 zip 缺工作区目录，修复要落在版本目录内，升级即失效——这类 repair 放进 skill 做成可重放清单是正确姿势
- 自动生成的中间文件（如每日事项汇总）应走独立通道（scp 直传）而非 git 仓库，避免污染笔记仓库
- 双端日记架构确认：服务器侧已有独立日记生成任务（23:50），Mac 本机 cron 的正确姿势是**合并写入**而非覆盖——本次已按 09-18 模式合并双端内容

## 今日摘要
基础设施域名化双杀日：Hermes Dashboard 与 dsh 先后完成公网域名接入（CF 源站证书 + nginx 反代 + Tailscale 隧道），顺手修掉 CN 版发行包缺 ui-tui 工作区导致的 Chat unavailable 硬核 Bug；重构了 Hermes 每日事项同步链路（本地留档 + scp 直传，不再进 git）；评估后决定暂不启用 MemoryCore 记忆网关。Mac 本机侧自动化全线正常（教程检查无更新、代办推送、收藏静默、心跳安静），全天无人工对话。

## 📊 自动化统计
- 服务器侧捕获 Memory 数：0（服务器 memory/2026-09-19.md 不存在）
- Claude 记忆：0（memory/claude_2026-09-19.md 不存在）
- 笔记仓库近 24h 更新：0（无新增 .md）
- Hermes 本机素材：1（diary_inbox/hermes_2026-09-19.md，5 个会话，601+ 条消息）
- Todoist 完成任务数：0
- Todoist 当日到期任务：0
- Claude Code 会话数：0
- Mac 本机 memory：1 条（2026-09-19.md：教程同步检查记录）
- Mac 本机人工对话：0（飞书/微信通道）
